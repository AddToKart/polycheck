// This screen is never shown — the "More" tab always opens the bottom sheet
// via the tabPress listener in _layout.tsx. This file exists so Expo Router
// registers the route.
export { default } from './dashboard'
